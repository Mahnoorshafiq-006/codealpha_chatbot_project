# codealpha_chatbot_project
An FAQ chatbot that answers customer queries using NLP — built with NLTK for text preprocessing, TF-IDF vectorization, and cosine similarity to match user questions to the closest FAQ. Covers order tracking, cancellations, refunds, payments, account security, and product availability. Runs via a simple Python input/output console interface.
